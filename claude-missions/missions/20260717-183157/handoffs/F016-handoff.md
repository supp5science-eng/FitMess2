# Handoff: F016 — Onboarding summary + persist targets

## Status
COMPLETE

## Assertions covered
AS-020: PASS — the summary at `/onboarding/pregled` renders the F014-computed daily/weekly kcal budget and macros for the wizard's hand-off data, and every input (pol, godine, visina, težina, nivo aktivnosti, ciljna težina, rok) is editable inline and recomputes the budget live before saving. Proven at three levels: (1) unit tests for the pure recompute helper (`src/lib/onboarding/__tests__/summary.test.ts`, `computeBudgetSummary` — editing activity/sex/weight/goal each demonstrably changes the output) and for server-side re-validation/write shape (`src/lib/onboarding/__tests__/persist.test.ts`); (2) component tests (`src/components/onboarding/__tests__/summary-screen.test.tsx`) driving real DOM edits (clicking a different activity tier, changing weight, clearing a required field) and asserting the on-screen `daily-kcal`/macro numbers change accordingly, plus that a failed save shows an inline Serbian retry without blanking the screen; (3) a live integration test (`src/app/(app)/onboarding/pregled/__tests__/actions.integration.test.ts`, `test_AS_020_persisting_edited_inputs_saves_the_recomputed_edited_budget_not_the_original`) that persists once, edits the activity level, persists again against the real Supabase project, and asserts the `targets.daily_kcal` row actually changed; and (4) a real-browser, 375px, end-to-end Puppeteer walkthrough (`evidence/F016/00-summary-populated-375px.png` → `01-summary-after-editing-activity-level-375px.png`, `evidence/F016/puppeteer-walkthrough.log`) that clicks "Veoma aktivan" on a live populated summary and captures the on-screen budget changing from 1843 to 2384 kcal, then confirms the *edited* value (`activity_level: "very_active"`, `daily_kcal: 2384`) is exactly what landed in the real `profiles`/`targets` rows.
AS-031: PASS — confirming the summary sets `profiles.onboarded_at` (non-null) and inserts the first `targets` row, and a returning (freshly re-signed-in) user is let through to `/danas` by the F013 middleware instead of being bounced back to `/onboarding`. Proven at three levels: (1) unit tests for `persistOnboarding`'s write shape against a mock Supabase client (`persist.test.ts`); (2) a live integration test against the real project (`actions.integration.test.ts`) that creates a verified-but-not-onboarded user, confirms the F013 gate still redirects `/danas` → `/onboarding` beforehand, calls `persistOnboarding` with a real signed-in session client, then asserts `profiles.onboarded_at` is non-null with the correct sex/height/weight/activity_level, a `targets` row exists with `weekly_kcal === daily_kcal * 7` and the submitted goal/timeframe, and — critically — that a **brand-new cookie jar signing in fresh** (exactly what "a returning user" means) now gets `response.headers.get("location") === null` for `/danas` (no redirect at all); (3) the same real-browser Puppeteer walkthrough's final admin-client check (`evidence/F016/puppeteer-walkthrough.log`) confirms the identical live-DB shape after a real click-through "Započni" confirm.

## Files changed
src/lib/onboarding/summary.ts (new) — pure helpers: `parseOnboardingSearchParams` (reads the F015 query-string hand-off, never throws on malformed/missing params), `isOnboardingDataComplete` (empty-state gate), `computeBudgetSummary` (runs a complete `OnboardingData` through the F014 engine: bmr → tdee → planGoalAdjustment → macroTargets, the single code path every render/edit uses), `explainGoalAdjustment` (AS-030 Serbian explanation copy for the engine's machine-readable `reasonCodes`)
src/lib/onboarding/persist.ts (new) — `persistOnboarding` (re-validates every field server-side via F015's own validators, recomputes the budget via F014, then UPDATEs `profiles` (sex/birth_year/height_cm/weight_kg/activity_level/onboarded_at) and INSERTs a `targets` row, using the caller's session-bound RLS client — extracted from the `"use server"` action specifically so it's directly testable against a live signed-in client without a Next request context) and `ageYearsToBirthYear`
src/lib/onboarding/__tests__/summary.test.ts (new) — 20 unit tests
src/lib/onboarding/__tests__/persist.test.ts (new) — 9 unit tests against a mock Supabase client
src/app/(app)/onboarding/pregled/page.tsx (new) — the route F015's wizard hands off to (`buildOnboardingSummaryUrl`); Server Component reads `searchParams`, renders the Serbian empty state for an incomplete hand-off or `<SummaryScreen>` for a complete one
src/app/(app)/onboarding/pregled/actions.ts (new) — `"use server"` `saveOnboardingAction`: resolves the signed-in user via the session-bound `createClient()`, delegates to `persistOnboarding`, `redirect("/danas")` on success
src/app/(app)/onboarding/pregled/__tests__/page.test.tsx (new) — 3 component tests (populated + two empty-state variants)
src/app/(app)/onboarding/pregled/__tests__/actions.integration.test.ts (new) — live AS-020/AS-031 proof against the real Supabase project + F013 middleware
src/components/onboarding/summary-screen.tsx (new) — the editable summary UI: big-friendly-number budget card, macro cards, AS-030 goal-adjustment notice, every field reusing F015's `OptionGroup`/`NumberField`/`FieldError` components and validators, `useTransition`-driven save with inline Serbian retry on failure
src/components/onboarding/__tests__/summary-screen.test.tsx (new) — 12 component tests
missions/20260717-183157/handoffs/evidence/F016/* (evidence: 3 real-browser 375px screenshots, puppeteer walkthrough log with live DB verification, test/lint/typecheck/build output logs)

No existing files were modified — this feature only adds new files under `/onboarding/pregled` + shared `src/lib/onboarding/`, reusing F015's existing wizard components (`OptionGroup`, `NumberField`, `FieldError`) and validators without changing them, consistent with the clarified "Touches: New page/components + reads the relevant lib/API" scope.

## Commands run
`npx vitest run "src/lib/onboarding" --reporter=verbose` (0) — 72 passed
`npx vitest run "src/components/onboarding/__tests__/summary-screen.test.tsx"` (0) — 12 passed
`npx vitest run "src/app/(app)/onboarding/pregled"` (0) — page tests (3) + live integration test (4 passed, 1 diagnostic skipped since credentials were present)
`npm run test` (0) — full suite: 27 test files, 281 passed, 7 skipped — see evidence/F016/test-output.log
`npm run lint` (0) — see evidence/F016/lint-output.log
`npm run typecheck` (0) — see evidence/F016/typecheck-output.log
`npm run build` (0) — `/onboarding/pregled` compiles as a dynamic route (reads `searchParams`) — see evidence/F016/build-output.log
`node <script>` — Management API / admin client: pre- and post-test-run sweeps confirming zero leftover `f016-*@example.com` test users (both the integration test's own `afterAll` cleanup and the separate Puppeteer walkthrough's explicit `deleteUser` were verified to actually remove every user they created)
`npm run build && npm run start -- -p 3101` + puppeteer-core (Edge, headless) driving a real sign-in through `/prijava`, a direct navigation to `/onboarding/pregled?pol=...` (the exact hand-off URL F015's wizard produces), a real click on a different activity-level radio button, and a real click on "Započni" — capturing 3 screenshots and the live-DB state via the admin client before/after (evidence/F016/puppeteer-walkthrough.log, evidence/F016/00-*.png..02-*.png). puppeteer-core + `@supabase/supabase-js` installed only in the session scratchpad, not the repo, per the F005/F011/F015 precedent.

## Decisions made
- **`persistOnboarding` is a plain function taking a `SupabaseClient<Database>` + `userId` + `OnboardingData`, factored out of the `"use server"` `saveOnboardingAction`.** This mirrors F011's `signInEmailPassword`/`src/lib/auth/core.ts` shape and is what let the live AS-020/AS-031 integration test call it directly against a real signed-in cookie-jar client (built the exact way F013/F015's own integration tests do), without needing a running Next server / `next/headers` request context that a plain Vitest process doesn't have.
- **Every field is re-validated and the budget is fully recomputed server-side inside `persistOnboarding`, ignoring whatever the client last rendered.** This is the "Money-math rule" (tech-decisions.md): the client's live recompute (`computeBudgetSummary`, called from `useMemo` on every edit) is purely a UI preview: the value that actually gets written to `targets` is always freshly derived from the six validated raw inputs via the same F014 engine functions, never trusted as a pre-computed number sent over the wire. Confirmed live: the integration test and the Puppeteer walkthrough both persist an *edited* set of inputs and verify the DB row's `daily_kcal` matches what the engine computes for those edited inputs, not a stale client value.
- **`saveOnboardingAction` is called directly as a plain async function from the client `SummaryScreen` (via `useTransition`), not through a `<form action>`.** There is no single natural `FormData` shape for a screen where every field is independently editable client state that also drives a live recompute (unlike F011's login/signup forms, which post once). Next.js Server Actions support being invoked as a plain function from client code exactly like this; `redirect()` inside it still works as a client-side navigation on success.
- **Chose "Započni" (not "Sačuvaj") as the primary confirm button label** — the clarified spec offered either; "Započni" ("get started") reads more naturally as onboarding's actual finish line into the app, matching the wizard's own "Završi" pattern from F015.
- **State on the summary screen is ordinary nullable `OnboardingData` (the exact same shape F015's wizard uses), not a "complete-only" type**, even though the page only ever mounts `SummaryScreen` with complete initial data. Clearing a field mid-edit (e.g. deleting the age input) degrades to the same per-field Serbian validation error the wizard shows, hiding the (now-invalid) budget preview via `isOnboardingDataComplete` + the six validators, rather than crashing `computeBudgetSummary` on a `null`.
- **AS-030's Serbian explanation text lives entirely in `explainGoalAdjustment` (UI layer), never in `engine.ts`** — this matches the F014 engine's own doc comment ("The Serbian explanation copy lives in the UI layer (F016), not here") and keeps the reason-code → sentence mapping unit-testable independent of any component.
- **`ageYearsToBirthYear` is a plain `Date`-based subtraction, not routed through a `src/lib/dates.ts` Europe/Belgrade utility** — tech-decisions.md's "never raw `new Date()` day math" rule is specifically about day/week *boundary* calculations (streaks, weekly resets); converting an already-collected age into an approximate birth year is a one-off arithmetic conversion, not a boundary computation, and no `src/lib/dates.ts` exists yet in this repo (it's listed in the planned file layout but not built by any feature so far).

## Out-of-scope work needed
- **`/danas` (the actual home screen) still does not exist** (confirmed live: after a successful confirm, `saveOnboardingAction`'s `redirect("/danas")` does fire server-side — the DB write is provably correct via the admin client immediately afterward — but since `/danas` itself 404s, the Puppeteer walkthrough observed the browser silently staying on `/onboarding/pregled`'s URL/DOM rather than showing a broken 404 screen (`evidence/F016/02-after-confirm-redirected-375px.png`). This is the same "redirect to a route that isn't built yet" situation F013/F015 both flagged as expected/out-of-scope for their own features; worth a quick look once M3 actually builds `/danas`, to confirm a real target route makes the client-side navigation from a directly-invoked (non-form) Server Action redirect visibly complete the way it does for the wizard's `router.push` pattern. Not a regression risk to any assigned assertion here — the DB write and the F013 middleware gate are both independently and live-verified without depending on `/danas` rendering anything.
- **No other gaps** — this feature's scope (editable summary + F014 recompute + persist profiles/targets + onboarded_at) is fully closed. The `plan.md` "Files (approximate)" note mentioned a single page + server action; this ended up as two small library modules (`summary.ts`, `persist.ts`) plus the page/action/component, split out specifically so both the live-DB write and the pure recompute math could be unit-tested in isolation from Next's request lifecycle — a structural choice, not a scope change.

## Blockers
(none — Status is COMPLETE)

## Autonomous decisions
AUTONOMOUS_DECISION: Chose "Započni" over "Sačuvaj" for the confirm button label (clarified spec offered either) — matches the wizard's own "Završi" finish-line pattern from F015.
AUTONOMOUS_DECISION: Invoked `saveOnboardingAction` as a direct function call from client state (via `useTransition`) rather than a `<form action>` with `useActionState` (the pattern every prior auth feature used) — the editable-summary-with-live-recompute shape has no natural single `FormData` submission the way a one-shot login/signup form does; documented in "Decisions made" above.
AUTONOMOUS_DECISION: Kept `ageYearsToBirthYear` as a plain `new Date()` read rather than inventing a `src/lib/dates.ts` utility that doesn't exist yet in this repo — this is a one-off arithmetic conversion, not a day/week-boundary calculation, so it doesn't fall under tech-decisions.md's "never raw `new Date()` day math" rule (which targets streaks/weekly-reset logic specifically).

## Notes for the next worker
- **`src/lib/onboarding/summary.ts` and `src/lib/onboarding/persist.ts` are the reusable pieces future budget-recalculation features (F045 weekly auto-recalc, F046 manual adjustment) should look at before reinventing this shape** — `computeBudgetSummary` is exactly "run these six inputs through the F014 engine and get back a full budget+macros+goal-adjustment bundle," and `persistOnboarding`'s "insert a new `targets` row, never update in place" pattern is the same one those later features need (per the `targets` migration's own comment: "every recalculation ... inserts a new row rather than updating in place").
- **MCP tools were not available** (`mcp__supabase__*` confirmed not bound in this worker subagent session either, consistent with every prior feature's documented finding). All live checks (creating/deleting test users, reading real `profiles`/`targets` rows, real middleware redirect headers) used `@supabase/supabase-js`'s admin client and the same cookie-jar RLS-client pattern F013/F015 established directly.
- **The Puppeteer walkthrough script (`f016-shots.mjs`) is not committed** (evidence artifacts are; the script that produced them is not), matching every prior feature's documented practice — it lives in the session scratchpad alongside F001/F011/F015's equivalents.
- **`GoalStep`/`OptionGroup`/`NumberField`/`FieldError` were reused as-is from F015 with zero edits** — the summary screen's "Pol" and "Nivo aktivnosti" sections are literally the same `OptionGroup` component F015's wizard steps use, just fed the summary's own state instead of the wizard's, which is what kept this feature's `git diff` free of any changes to existing files.
