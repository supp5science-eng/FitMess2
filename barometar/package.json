{
  "name": "barometar",
  "version": "0.0.1",
  "private": true,
  "engines": { "node": "22.x" },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5",
    "vitest": "^4.0.0"
  }
}
